<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog x:Class="Starward.Features.GameLauncher.DX12IntroDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:cwc="using:CommunityToolkit.WinUI.Controls"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:lang="using:Starward.Language"
               xmlns:local="using:Starward.Features.GameLauncher"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:sc="using:Starward.Controls"
               x:DefaultBindMode="OneWay"
               Background="{ThemeResource CustomAcrylicBrush}"
               BorderThickness="0"
               CornerRadius="8"
               Style="{ThemeResource DefaultContentDialogStyle}"
               mc:Ignorable="d">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1200</x:Double>
        <Thickness x:Key="ContentDialogPadding">0</Thickness>
        <StaticResource x:Key="ContentDialogSmokeFill" ResourceKey="ControlFillColorTransparentBrush" />
        <StaticResource x:Key="ContentDialogTopOverlay" ResourceKey="ControlFillColorTransparentBrush" />
    </ContentDialog.Resources>


    <Grid MaxWidth="680" Padding="24">



        <StackPanel Spacing="20">
            <TextBlock FontSize="20"
                       FontWeight="Bold"
                       Text="{x:Bind lang:Lang.DX12IntroDialog_DX12EffectDetails}" />

            <StackPanel HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="20">
                <Grid Width="300"
                      Height="225"
                      CornerRadius="16">
                    <sc:CachedImage Source="{x:Bind GameDXConfig.DX11PreviewImage}" />
                    <Border Padding="0,4,0,4"
                            VerticalAlignment="Bottom"
                            Background="{ThemeResource ControlOnImageFillColorSecondaryBrush}">
                        <TextBlock HorizontalAlignment="Center"
                                   FontWeight="Bold"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="{x:Bind lang:Lang.DX12IntroDialog_RegularGraphics}" />
                    </Border>
                </Grid>
                <Grid Width="300"
                      Height="225"
                      CornerRadius="16">
                    <sc:CachedImage Source="{x:Bind GameDXConfig.DX12PreviewImage}" />
                    <Border Padding="0,4,0,4"
                            VerticalAlignment="Bottom"
                            Background="{ThemeResource ControlOnImageFillColorSecondaryBrush}">
                        <TextBlock HorizontalAlignment="Center"
                                   FontWeight="Bold"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Text="{x:Bind lang:Lang.DX12IntroDialog_DX12EnhancedGraphics}" />
                    </Border>
                </Grid>
            </StackPanel>

            <TextBlock FontSize="16"
                       Text="{x:Bind GameDXConfig.I18nIntro}"
                       TextWrapping="Wrap" />
        </StackPanel>



        <!--  Close Button  -->
        <Button Width="32"
                Height="32"
                Margin="-20"
                Padding="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                BorderThickness="0"
                Command="{x:Bind CloseCommand}"
                CornerRadius="8"
                Style="{ThemeResource DateTimePickerFlyoutButtonStyle}">
            <FontIcon FontSize="16"
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                      Glyph="&#xE711;"
                      IsTextScaleFactorEnabled="False" />
        </Button>


    </Grid>

</ContentDialog>
